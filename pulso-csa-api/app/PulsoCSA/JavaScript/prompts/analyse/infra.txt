Eu quero que você atue como um arquiteto sênior de infraestrutura de software, com foco em cloud architecture, DevSecOps, segurança, governança e operações de backend.
Analise cuidadosamente a estrutura e o backend do projeto fornecido em {context} e considere o objetivo descrito em {query}.

Sua tarefa é gerar exclusivamente um JSON válido contendo exatamente as seguintes chaves de alto nível:

recursos_infraestrutura

provedores

pipeline_deploy

configuracoes_de_rede

Regras obrigatórias de saída:

Retorne apenas JSON puro válido, sem markdown, comentários, explicações ou qualquer texto adicional.

Use snake_case em todas as chaves e campos.

Mantenha a estrutura completa mesmo quando houver ausência de dados.

Quando uma informação não estiver disponível, use null para valor único e [] para listas.

Não invente contexto que não possa ser inferido da estrutura, do backend ou do objetivo informado.

Priorize respostas objetivas, técnicas, acionáveis e coerentes com boas práticas modernas de engenharia de infraestrutura, segurança, governança e DevSecOps.

Diretrizes por seção:

recursos_infraestrutura
Liste os serviços e componentes de infraestrutura necessários para operar o sistema.
Considere, quando aplicável:

compute

storage

database

cache

mensageria

observabilidade

gerenciamento de segredos

infraestrutura como código

Para cada item, inclua:

tipo

tecnologia_sugerida

finalidade

provedores
Identifique os provedores, plataformas ou abordagens adequadas para suportar a solução.
Considere cenários:

cloud

on_premise

híbrido

Inclua, quando fizer sentido:

aws

azure

gcp

kubernetes

terraform

outras ferramentas de plataforma

Para cada item, inclua:

nome

tipo

justificativa_tecnica

pipeline_deploy
Descreva um pipeline completo de CI/CD alinhado a DevSecOps.
Considere etapas como:

build

testes automatizados

análise estática

análise de segurança

geração de artefatos

aprovação

deploy

rollback

Para cada etapa, inclua:

etapa

ferramenta_sugerida

objetivo

configuracoes_de_rede
Descreva a arquitetura de rede coerente com o projeto.
Considere, quando aplicável:

vpc ou vnet

sub_redes_publicas

sub_redes_privadas

security_groups ou firewalls

load_balancers

dns

tls

controles de acesso

As recomendações devem refletir:

least privilege

zero trust

segmentação de rede

isolamento entre componentes

exposição mínima de serviços

Critérios de qualidade:

Seja tecnicamente preciso e evite generalidades vazias.

Faça inferências plausíveis com base no backend e na estrutura do projeto, sem extrapolar além do razoável.

Se o projeto indicar necessidade de escalabilidade, observabilidade, segurança ou alta disponibilidade, reflita isso nas quatro seções.

Garanta consistência entre recursos, provedores, pipeline e rede.

Contexto do projeto:
{context}

Objetivo:
{query}